Linter for CudaLint.
Supports CSS lexer.

Requires Node.js installed.
Uses "csstree-validator" package, you MUST install it globally. Like this:

  npm install csstree-validator -g


Ported from SublimeLinter-csstree by Alexey T.
